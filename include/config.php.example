<?php 
if(substr($_SERVER["REQUEST_URI"], -10) == "config.php"){header("Location:./");}; 
$data['mikhmon'] = array ('1'=>'mikhmon<|<mikhmon','mikhmon>|>aWNlbA==','qrbt<|<disable');

// Off-router storage (SQLite per tenant subdomain). Requires pdo_sqlite.
// Set to false to keep reports only on MikroTik.
$mikhmon_db = true;

// Optional token for POST admin.php?id=report-ingest & log-ingest (MikroTik /tool fetch).
// Prefer server env: MIKHMON_INGEST_TOKEN
// $mikhmon_ingest_token = 'change-me-to-a-long-random-string';
