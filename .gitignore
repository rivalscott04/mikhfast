mikhmon_ai_guidelines.zip
mikhmonv3-master.zip
mikhmon_ai_guidelines/

# Live data — jangan di-commit / jangan di-overwrite git pull
include/config.php
include/config.php.bak
include/quickbt.php
voucher/templates/*
!voucher/templates/.gitkeep

# Build output (generated on server via bun run build)
js/mikhmon.bundle.prod.js
node_modules/

# graphify knowledge graph (generated)
graphify-out/

# Tests (local / CI only — not committed)
tests/

# Tenant SQLite databases (generated per subdomain)
data/tenants/**/*.sqlite
data/tenants/**/*.sqlite-*
data/tenants/**/config.php
data/tenants/**/config.php.bak
data/superadmin/credentials.json

# Local env files (never in document root / never commit secrets)
.env
.env.local
.env.*.local
